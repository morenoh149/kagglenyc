doctype html
html
  head
    include _head_common.pug
    title #{__('TITLE', community)}
  body
    include _nav.pug
    .wrapper
      .main
        .header
          h1
            strong #{community}
          h2(class="#{isFailed?'error':''}") !{message}
